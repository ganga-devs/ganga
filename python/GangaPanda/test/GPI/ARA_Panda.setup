
import os
tmpdir = '/tmp/ganga_ara_panda_testing_data-$USER'
print 'copying ARA Panda testing file to ',tmpdir
os.system('rm -rf %s'%tmpdir)
os.system('cp -r /afs/cern.ch/sw/ganga/external/test-externals/1.0/noarch/ATLAS_Panda_ARA %s'%tmpdir)
