pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    // Build stage
    stage('Build Core Image') {
      steps {
        sh """tagNo=\$(git describe)
        echo $tagNo
        """
      }
    }
  }

}
