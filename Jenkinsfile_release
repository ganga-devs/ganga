pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    // Build stage
    stage('Build Core Image') {
        steps{
		withCredentials([usernamePassword(credentialsId: 'PyPi', passwordVariable: 'PYPIPASSWORD', usernameVariable: 'PYPIUSER'), string(credentialsId: '3c8f4c29-6a7e-4b89-9735-de6c6ad4540e', variable: 'GITHUBAPITOKEN')]) {
        sh "./startRelease.sh"
	}
      }
    }
  }

}
