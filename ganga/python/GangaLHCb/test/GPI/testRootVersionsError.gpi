#assign dict - should work
d = {'5.14.00i':'v19r9'}
config.DIRAC.RootVersions = d

j = Job(application = Root(version = '5.14.00i'),backend = Dirac())

try:
    #should throw an error
    config.DIRAC.RootVersions = 'dsknkdlnsklakdlnak'
    assert False, 'ConfigError should be thrown'
except ConfigError:
    assert config.DIRAC.RootVersions == d, 'Should roll back to previous state'

j.remove()