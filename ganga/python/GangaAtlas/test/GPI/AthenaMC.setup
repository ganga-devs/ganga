
import os
tmpdir = '/tmp/ganga_athenaMC_testing_data-$USER'
print 'copying AthenaMC testing data to ',tmpdir
os.system('rm -rf %s'%tmpdir)
os.system('cp -r /afs/cern.ch/sw/ganga/install/TEST/athenaMC_testing_data %s'%tmpdir)

